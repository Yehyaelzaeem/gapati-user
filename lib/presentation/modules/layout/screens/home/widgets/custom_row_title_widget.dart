import 'package:flutter/material.dart';

class CustomRowTitleWidget extends StatelessWidget {
const CustomRowTitleWidget({Key? key}) : super(key: key);

@override
Widget build(BuildContext context) {
return Container();
}
}
